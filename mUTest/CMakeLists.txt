# new_executable(mutest)
# add_common_to_target(mutest)
# target_link_libraries(mutest mukernel)
# target_link_libraries(mutest ${Boost_LIBRARIES})
# target_link_libraries(mutest ${gmp_LIBRARIES})

add_subdirectory(sample)